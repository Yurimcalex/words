export default function Search() {
	return (
		<div className="search dark-2">
			<input 
				className="dark-2"
				type="text"
				// value={searchText}
				// onInput={(e) => setSearchText(e.target.value)}
			/>
		</div>
	);
}